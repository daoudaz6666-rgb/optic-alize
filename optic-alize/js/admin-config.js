/* ============================================================
   OPTIC ALIZE - identifiants de l'administrateur

   Pour changer les identifiants : modifiez les deux valeurs
   ci-dessous, puis enregistrez le fichier.

   /!\ IMPORTANT : sur un site 100% statique (sans serveur), ce
   fichier est TELECHARGEABLE par n'importe qui. Ces identifiants
   n'offrent donc PAS une vraie protection. Pour un vrai espace
   admin securise, il faut un backend (voir js/admin.js en tete).
   ============================================================ */

const ADMIN_CONFIG = {
  identifiant: "admin",
  motDePasse: "optic-alize-2026",
};
